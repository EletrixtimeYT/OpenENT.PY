from setuptools import find_packages, setup

setup(
    name='OpenENTpy',
    packages=find_packages(),
    version='0.0.1',
    description='A python library for interacting with OpenENT API !',
    author='EletrixTime',
)